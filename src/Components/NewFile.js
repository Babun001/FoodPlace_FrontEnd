import React, { createContext, useReducer } from 'react';

const CartStateContext = createContext();
const CartStateContext = createContext();

const reducer = (state,action)=>{
    
}

export const Cartfunc = ({child}) =>{
    const [state,dispatch] = useReducer()
    return (

    )
}
